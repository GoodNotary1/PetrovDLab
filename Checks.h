#pragma once
#include <string>
#include <limits>
using namespace std;

int intCheck(int min = 0, int max = INT_MAX);

string stringCheck();

float floatCheck(float min = 0, float max = FLT_MAX);

bool booleanCheck();